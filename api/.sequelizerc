const path = require('path');
const config = require('config');

const environment = config.get('nodeEnv');
const databasePath = environment === 'development' ? './src/db/' : './src/db/';

module.exports = {
  config: path.resolve(`./config`, 'config.js'),
  'seeders-path': path.resolve(`${databasePath}seeds`, environment),
  'migrations-path': path.resolve(`${databasePath}migrations`, environment),
};
